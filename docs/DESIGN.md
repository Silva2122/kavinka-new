# КАВИНКА — дизайн-направление

⚠️ **Этот файл переписан 2 августа 2026 по факту утверждённого hero.** Первая версия документа (написана до макетирования) во многом устарела — например, там сказано «скруглений нет», а утверждённый дизайн активно использует пилюли и скруглённые карточки. Верь этому файлу и файлу [design/page.html](../design/page.html), а не своим ожиданиям из старых записей.

---

## 0. Где смотреть

Дизайн сейчас существует как **отдельные статичные HTML-макеты в `design/`**, ещё не портирован в реальные Next.js компоненты (`app/`, `components/`). Это осознанный этап — сначала утверждаем визуал с Боссом на быстрых HTML-прототипах, потом переносим в React.

| Файл | Статус | Что там |
|---|---|---|
| [design/page.html](../design/page.html) | **Рабочий, живой файл** | Единственный источник правды по текущему дизайну. **Вся главная страница утверждена целиком**: hero, «Быстрые факты», «О базе», «Виды рыб», CTA, футер, закреплённый хедер, анимация появления при скролле. Следующая задача — вторая страница, см. `CLAUDE.md` §5 |
| `design/hero-v1.html` … `hero-v8.html` | Архив итераций | История поиска hero. v8 — финальная версия, легла в основу `page.html`. Не удалять, там видно, что уже пробовали и отвергли |
| `design/about-v3.html` | Архив итераций | История поиска «О базе» — от фото во весь экран с разбросанными карточками до финальной вёрстки (текст слева-сверху, «пропуска» у правого края). Совпадает с тем, что сейчас в `page.html` |
| `wireframes/*.html` | Архив, до-визуальный этап | Серые вайрфреймы и раскадровки концепций. Боссу не понравились — «слишком дефолтно», см. `DECISIONS.md`. Работу вели дальше сразу в цвете |

**Следующий шаг по главной целиком:** перенести в реальные Next.js-компоненты (`components/sections/`), завести токены и переменные шкалы (раздел 10) из `page.html` в `app/globals.css` — они там пока **не совпадают** со старыми (см. раздел 3).

**Следующий шаг по проекту:** вторая страница сайта — см. `CLAUDE.md` §5. Вести её так же итеративно: конкретные приёмы из `ref/`, не абстрактные концепции (раздел 9), переиспользовать утверждённые компоненты (разделы 8, 9, 10), а не изобретать заново.

---

## 1. Референсы

Лежат в [ref/](../ref/), присланы Боссом.

| Файл | Что за сайт | Что взяли |
|---|---|---|
| `image copy.png`, `image copy 2.png` | «Золотое», производитель яблок | Болотно-зелёный + кремовый, крупная антиква капсом, двухцветные заголовки, чередование светлых и тёмных секций |
| `image copy 3.png` | Женский тренинг в Италии | Болотный + светлый, карточки поверх фото |
| `image copy 5.png` | «Букетная», цветочная мастерская | Кремовый фон вместо белого |
| `image.png`, `image copy 4.png` | Разное | Структура карточек с ценой (пригодится для «Стоимости») |

Референсы мягкие (арки, пилюли, вензеля) — это женская аудитория. У нас рыболовная база, аудитория мужская. **Важная поправка в процессе:** первая версия этого документа советовала выкинуть скругления и пилюли совсем — Босс потом явно попросил вернуть мягкость (скруглённые кнопки, пилюля координат), совместив её с камуфляжем и брутальными деталями. Итоговая формула — не «брутализм», а **тёплая природная палитра с камуфляжем как единственным жёстким акцентом**, подробнее в разделе 5.

Референс заглушки от Босса: **`https://silva2122.github.io/kavinka/`** — его собственный черновик. Оттуда взят приём стеклянной пилюли для координат (`blur`, полупрозрачный фон, точка-разделитель) и общий язык «стекла» для кнопок.

---

## 2. Камуфляж — фирменный приём

Файл: `public/photo/camo.jpg`.

На прошлом сайте Босс наложил камуфляж на прозрачную кнопку — заказчику очень понравилось. В утверждённом hero это развито в две парные системы:

- **На тёмном фоне** (hero-панель) — класс `.glass-btn`: полупрозрачное стекло (`backdrop-filter: blur`), камуфляж отдельным слоем на **34% opacity** в покое (было 62% — резалось в текст, пришлось приглушать), кремовая вуаль поверх усиливает читаемость, при hover камуфляж уплотняется до 48%.
- **На светлом фоне** (кнопка «Подробнее» в «О базе») — класс `.camo-btn`: камуфляж плотнее, тёмная вуаль `rgba(35,35,24,.5)` поверх для контраста с кремом.

Более ранняя идея — заливать камуфляжем весь фон панели или делать «рваный бумажный переход» между секциями зелёного цвета — **опробована и отклонена**, см. раздел 6.

Правило, подтверждённое на практике: камуфляж — **акцент на маленьких объектах** (кнопка), а не на больших площадях. На всей панели он либо невидим (слишком блёкло), либо рябит.

---

## 3. Палитра — актуальная, из `design/page.html`

⚠️ Отличается от значений в `app/globals.css` — при переносе в Next.js нужно **заменить токены**, не сливать со старыми.

```css
--cream:      #F0EFE4;  /* основной светлый фон секций */
--cream-warm: #E7E2CE;  /* фон бегущей строки — чуть темнее cream */
--bog:        #242B1B;  /* тёмные секции: hero-панель, "Виды рыб" */
--sage:       #B7C299;  /* второй цвет двухцветных заголовков, светлые акценты на тёмном */
--olive:      #7C8C57;  /* акцент на светлом: иконки, крупные цифры, точки */
--ink:        #232318;  /* текст на светлом фоне, тёплый почти-чёрный */
--muted:      #6E6D5E;  /* второстепенный текст */
```

Старые токены `sand`, `moss`, `bark`, `stone`, `olive-light` из первой версии документа и `app/globals.css` **не используются** в утверждённом hero — считать их устаревшими черновиками, не смешивать.

**Emerald по-прежнему отменён** — старый акцент прототипа `#059669` отношения к текущей палитре не имеет.

---

## 4. Типографика — два шрифта, не три

```css
--disp: 'Cormorant Garamond', Georgia, serif;   /* заголовки */
--body: 'Manrope', system-ui, sans-serif;        /* всё остальное, включая "технический" слой */
```

**JetBrains Mono отменён.** Первая версия дизайна планировала третий, моноширинный шрифт для координат/цен/дат. На практике это резало глаз — «слишком много разных шрифтов». «Технический» вид (координаты, цена, кнопки) теперь даёт не отдельная гарнитура, а **Manrope капсом с трекингом**:

```css
--meta-size: 14px;
--meta-tracking: .12em;
--base-size: 18px;  /* обычный текст абзацев */
```

### Эйбрау — капсом, межбуквенный интервал строго 0 (правило Босса, 14.09.2026)

Мелкий зелёный заголовок над H2 (`.eyebrow`, например «Кавинская долина», «Дорога до базы»):

```css
text-transform: uppercase;
letter-spacing: 0;       /* строго 0 — не разряжать, никаких других значений */
```

`--meta-tracking` к эйбрау **не применять ни в каком виде** — ни в модификаторах, ни на новых страницах, ни при переносе в React. Текст в разметке пишем обычным предложением (капс даёт CSS). Правило действует во всех `design/*.html`. За один день пробовали и строчными — Босс вернул капс, но без разрядки.

**Поплавок перед эйбрау** (Босс: «прикольный, пусть покачивается»): `.eyebrow::before` — силуэт поплавка 10×18px маской; цвета **только из палитры** — тело цветом эйбрау (`currentColor`), шапка тем же цветом светлее (`color-mix`), без оранжевого. Под ним рябь воды `.eyebrow>span::before`. Качается `eyebrowFloat` 3.8s, при `prefers-reduced-motion` стоит. Разметку не трогать — любой новый `.eyebrow` получает поплавок сам. SVG в `url("data:...")` кодировать с `%22`/одинарными кавычками — с сырыми `"` маска молча не применяется и рисуется прямоугольник.

**Отклонено:** плашка-пилюля вокруг эйбрау (заменена поплавком); оранжевая шапка поплавка («в цветах сайта»); тире перед эйбрау (`DECISIONS.md` №17).

### Антиква — только в заголовках секций (правило Босса, 14.09.2026)

`--disp` (Cormorant Garamond) остаётся **только** у: `.h2` (заголовки секций), `.page-head h1`, `.hero__brand`, `.hero__slogan`.

Всё остальное — `--body` (Manrope), **строчными с заглавной, без капса**: подписи карточек («Домика на базе»), названия рыб, коттеджей, пакетов, допуслуг, шаги маршрута, билеты, цитаты-врезки, цены и любые цифры. Насыщенность 500–600, не 700 — Босс просил «не так жирно». Причина: антиква в мелких подписях и цифрах читалась плохо, старостильные цифры «пляшут».

Новые карточки и компоненты сразу собирать на Manrope; `var(--disp)` вне перечисленных четырёх селекторов — ошибка.

Playfair Display тоже заменён на **Cormorant Garamond** — Playfair показался слишком предсказуемым («Inter среди антикв», буквально предупреждение из кукбука Anthropic про AI-generated дизайн). Cormorant даёт больше тепла, ближе к референсам Босса.

### Заголовок-слоган: два ряда со сдвигом

Фирменный приём hero, скопирован из заглушки Босса (`h1 strong{transform:translateX(...)}` в её CSS) и адаптирован под кириллицу:

```html
<h1 class="slogan-h1">
  <span class="ln">Пароль в мир</span>
  <span class="ln">дикой природы</span>
</h1>
```

Вторая строка сдвинута вправо через `transform: translateX(.85em)` — **в `em`, не в `vw`**. Это специально: при отдельном масштабировании шрифта и сдвига по ширине экрана на средних разрешениях (1024–1440px) слово банально обрезалось за край панели. Сдвиг в `em` растёт вместе со шрифтом и никогда не рассинхронизируется. Если будешь трогать эту вёрстку — не возвращай `vw`-сдвиг, это наступит на старые грабли.

Размер самого заголовка задан в два яруса под ширину экрана (не единым `clamp`, а с точкой перелома на `1100px`) — тоже защита от обрезания на средних ширинах. Детали — в самом `page.html`, класс `.slogan-h1 .ln` и медиа-запрос `min-width:1100px`.

### Двухцветный заголовок

```
О БАЗЕ       ← "БАЗЕ" цветом --sage
```

Приём из референсов, используется во всех H2.

---

## 5. Принципы вёрстки — обновлено по факту

1. **Скругления есть.** Пилюли у кнопок (`border-radius:100px`), у карточек — скромный радиус `6-8px`. Первая версия документа запрещала скругления полностью — это было ошибкой, Босс прямо попросил вернуть мягкость.
2. **Тени — по минимуму**, только мягкие (у кнопок `box-shadow` с большим blur), никаких резких.
3. **Один осознанный крупный элемент на экран** — не размазывать эффекты. В hero это заголовок-слоган; в остальных секциях крупный акцент ещё предстоит найти (см. раздел 7).
4. **Секции чередуются светлое/тёмное.** Актуальный порядок на главной: hero (тёмная, фото) → «Быстрые факты» (светлая, `--cream`) → «О базе» (тёмная, видео) → «Виды рыб» (светлая, `--cream`) → CTA (тёмная, видео) → футер (тёмная, `--bog`). «Виды рыб» специально сделана светлой (а не тёмной, как было в первой отклонённой версии) — иначе после «О базе» шли бы два тёмных видео-блока подряд и ритм ломался. Ритм светлое/тёмное из референса «Золотое», конкретное чередование — по факту того, что получилось на главной.
5. **Реальные факты вместо декора.** Если что-то похоже на украшение (нумерация, схема, диаграмма) — это должно быть основано на настоящих данных проекта (координаты, сезоны, реальная география), а не выдумано для красоты. Требование сформулировано явно после того, как Boss отверг фиктивный «календарь сезона» на этапе вайрфреймов.
6. **Единая шкала размеров.** Не подбирать «почти одинаковые» значения на глаз — по факту резало глаз как хаос, пришлось делать отдельный проход по всей главной, чтобы свести. `--meta-size` — служебные элементы (пилюля, кнопки, цена, киккер). Полная шкала для карточек/сеток/заголовочных блоков — раздел 10, переменные `--size-h3`/`--size-h3-sm`/`--size-copy`/`--size-small`/`--grid-max`/`--head-max`. Заводя новую секцию — брать размеры оттуда, не придумывать новые числа.
7. **Мобильная адаптация сделана 16.09.2026** на всех пяти `design/*.html`: полноэкранное меню по бургеру (строится скриптом из `.site-nav__links` страницы, кнопка и телефон внизу, бургер анимируется в крестик, Esc закрывает), единый ритм `--section-y:80px` на ≤640, кнопка в шапке прячется на ≤430 (она есть в меню). Список домиков на телефоне — цена под названием; карточки «Что включено» и «Важная информация» — иконка слева, текст справа; лента дней прокручивается вбок с растворённым правым краем. На ≤640 шкала на ступень меньше: `--base-size:16 · --size-h3:20 · --size-h3-sm:18 · --size-copy:14 · --size-small:12 · --meta-size:13`, H1 34 (Босс 16.09: десктопные 18/24 на телефоне крупноваты). Поля карточек 20–22 вместо 30, иконки-нашивки 52 вместо 66, кнопки 15/30 вместо 17/38, CTA и футер 64/56 вместо 96/86, фото туров 16:10 и фото дня 4:3 вместо 4:3 / 4:5 — чтобы карточки не тянулись. Проверка ширин <500 px — через iframe, см. `CLAUDE.md` §6.

---

## 6. Что уже пробовали и отклонили — не предлагать снова

- **Рваный бумажный переход между секциями** (SVG с неровным краем + процедурное зерно через `feTurbulence`, покрашенный то в собственный «бумажный» зелёный, то в цвет соседней секции). Технически сделано аккуратно и проверено на нескольких вариантах цвета, но Босс решил вообще убрать любые переходы между секциями — «не нужны ни в каком виде». Секции теперь стыкуются напрямую, без декоративной границы.
- **Заголовок «Пароль в мир дикой природы» в 4 строки** с сдвигом каждой строки вразнобой — Босс попросил вернуть к 2 строкам («Пароль в мир» / «дикой природы»), как в референсной заглушке.
- **Огромное имя «КАВИНКА» на всю панель hero** (было в ранних версиях v1–v5) — отклонено, имя переехало в маленький вордмарк в шапке, а место в hero занял слоган.
- **Кикер и лид-абзац как отдельные придуманные тексты** — оказалось, что часть текста в ранних версиях hero была сочинена, не взята из `STRUCTURE.md`. Исправлено: весь текст в утверждённом hero дословно из структуры или банка текста сайта.
- **Секция «О базе» в виде фото слева / текст справа**, фото в рамке 4:5 — забраковано как «дефолтно, шаблонно». Пробовали также вариант с фото на вылет к краю + карточками-фактами + мини-диаграммой слияния рек — **тоже забраковано** («отвратительно»), без уточнения деталей. Требуется полностью новый заход, не косметика.
- **Секция «Виды рыб»** (тёмная, сетка из 6 карточек с видео) — тоже названа «отвратительной» в той же реплике, без отдельных деталей. Тоже требует полного редизайна.

---

## 7. «Виды рыб» — решено (было открытым вопросом)

Раньше здесь был открытый вопрос («что конкретно не понравилось в „Видах рыб“?») — **закрыт**, секция утверждена. Финальное решение и весь путь итераций — раздел 10 и `DECISIONS.md` №17.

Сам метод, которым вопрос закрыли, стоит помнить для будущих секций: не гадать и не предлагать вариант вслепую — переспросить Босса, что конкретно не работает, и сразу собрать 2-3 конкретных приёма из референсов (не словесные концепции), дать выбрать. Это же сработало для «О базе» (раздел 9) и для «Виды рыб» (раздел 10) — рабочий процесс на будущее, включая вторую страницу.

---

## 8. Утверждённые компоненты hero (переиспользовать как есть)

Эти элементы Босс одобрил явно — не переизобретать:

- **Бегущая строка** (`.ticker`) вверху страницы, тон `--cream-warm`, едет бесшовно на паузу при наведении. Пункты — короткие факты дословно из `STRUCTURE.md` + слоган в кавычках, разделены точкой `•`, все капсом, без тире между заголовком и описанием.
- **Пилюля координат** (`.coords-pill`) — стекло, `blur(12px)`, точка-разделитель радиальным градиентом sage→olive.
- **Галерея трофейных фото** справа от текстовой панели — кроссфейд, точки-пагинация, автопрокрутка 5 сек, пауза при наведении, уважает `prefers-reduced-motion`.
- **Асимметричная сетка 50/50** панель/фото — не путать со старыми версиями с процентными пропорциями 44/56, точно 50/50.

---

## 9. Утверждённые компоненты «Быстрые факты» и «О базе» (переиспользовать как есть)

«О базе» прошла путь в несколько итераций за один заход — от словесных концепций (все отвергнуты как «слишком сложно» и «не идёт от референсов», см. `DECISIONS.md` №16) к конкретному приёму из присланного референса «Италия» (стемпы-модули программы тренинга). Рабочий процесс, который сработал и стоит повторить для «Виды рыб»: показывать 2-3 конкретных приёма **с привязкой к конкретной картинке из `ref/`**, а не абстрактные идеи, и делать HTML-мокап с реальным скриншотом на каждой правке, а не описывать словами.

**Быстрые факты** (между hero и «О базе», лёгкая пауза-выдох):
- Секция на мягком светлом фоне `.soft` (песочный + камуфляж ~5%) — эйбрау + H2 + лид по центру, затем 4 карточки в ряд.
- **Обновлено 14.09.2026.** Карточки тёмные болотные (`--bog`), камуфляж на них **едва заметен** (~4%, `screen`) — только фактура. Эффекта наведения нет: карточки некликабельные, Босс попросил убрать. Пробовали и отклонили: светлое «стекло» с камуфляжем (не красочно); яркий тёмный камуфляж (отвлекал от кнопки `.btn`); сплошной зелёный без камуфляжа (скучно). Камуфляж ярко — только у кнопки, она главный акцент.
- Внутри карточки: иконка-«нашивка» → крупная цифра Manrope 500 (+ «км» тонким серым) → подпись Manrope 600 строчными → описание. Антиквы и капса в карточке нет (см. §4).
- **Иконки — «охотничье-рыбацкие нашивки»**, не базовые line-иконки: тёмный кружок 66px, тонкое кольцо + пунктирная «строчка» по кругу, детальный значок цвета выгоревшей латуни `#D8CDA2`. Значки: сруб с трубой и сосной · лосось с плавниками и крапом · сопки с меткой · рыбак в лодке с удочкой. Новые иконки на сайте рисовать в этом же стиле.
- Текст карточек — дословно из `STRUCTURE.md` §3.2, не сочинять новые пункты.

**О базе** (полноэкранная видео-секция сразу после):
- Секция без вертикальных отступов (`padding:0 46px`) — фон стыкуется с hero сверху и с «Виды рыб» снизу напрямую, без зазора. Высота — `min-height:82svh`, ощутимо крупный блок.
- Фон на всю ширину экрана (`.about__stage`, bleed за счёт отрицательных полей у `.about` + `width:calc(100% + 92px)` у стейджа) — **сейчас видео** `lake-ripples.mp4` (было фото `base-from-water.jpg`, Босс попросил заменить на «оживлённую» версию той же сцены — по факту тот же кадр, сгенерированный в видео с рябью на воде). Разметка держит оба варианта: `.about__stage img,.about__stage video{...}` — общее правило, менять источник — просто заменить тег. Диагональный тёмный градиент сильнее слева (где текст) и мягче справа (где карточки на своём тёмном фоне).
- Текст (H2 сплошным `--cream`, без двухцветности — на фото зелёное слово сливалось с деревьями, поэтому здесь заголовок одноцветный, в отличие от карточек «Быстрых фактов») и кнопка `.glass-btn` (та же, что в hero) — в левом верхнем углу.
- Факты — «пропуска» (`.pass`): тёмные бог-зелёные плашки, друг под другом у правого края, зазор 6px, ширина одинаковая. Заголовок и подпись внутри карточки не центрированы и не растянуты — идут единым блоком сверху.
- Перфорация у пропусков — **настоящий CSS-вырез через `mask-image`/`mask-composite:intersect`** (два `radial-gradient`, один сверху, один снизу, пересечение вырезает волну по обеим кромкам), а не подделка цветом фона. Это принципиально: цветовая подделка ломается, если карточка на фото или повёрнута под углом — настоящая маска работает всегда, потому что реально прозрачна. Если понадобится похожий перфорированный край где-то ещё — переиспользовать этот же приём, не изобретать заново.
- Карточки **не крутить и не разбрасывать в стороны** (был промежуточный вариант с `rotate()` и разной высотой — отклонён явно: «почему поехали эти дырки», «как-то криво»). Ровный вертикальный стек — единственное утверждённое положение.

---

## 10. Утверждённые компоненты «Виды рыб», CTA, футер, хедер, анимация — вся остальная главная

### Виды рыб — список слева, видео справа (не сетка карточек)

Финальная форма радикально не похожа на обе отклонённые версии (см. `DECISIONS.md` №15, №17). Приём выбран из референса «Дагестан» (карточка-билет), но в процессе Босс сам предложил заменить сетку на интерактивный список:

- Слева — `.fish-list`: вертикальный список из 6 строк (`.fish-list__item`, это `<button>`), у каждой номер («01»…«06»), название рыбы, сезон, стрелка `→`, которая появляется при наведении/активности. Разделены тонкими линиями (`border-top`, только `rgba(35,35,24,.14)`), без заливки.
- Справа — `.fish-stage`: одна сцена с 6 видео, наложенными друг на друга (`position:absolute`), кроссфейд по `opacity` между `.is-active` — **тот же приём**, что в галерее трофеев hero (`.gallery__slide.is-active`), просто по наведению на список, а не по таймеру.
- Наведение (`mouseenter`) и клик на строку списка переключают активные класс у строки и у соответствующего видео (JS в конце `page.html`, ищет по `data-index`).
- **aspect-ratio сцены — `16/9`, не `4/3` и не `3/2`.** Пробовали оба — рыба (снятая широким кадром) обрезалась по бокам при не-16:9 контейнере, потому что источник видео буквально 16:9 (1920×1080) и `object-fit:cover` обрезает лишнее по короткой стороне. Если появятся видео другого нативного соотношения — держать `aspect-ratio` сцены равным соотношению исходника, иначе обрежет.
- Секция **светлая** (`--cream`), не тёмная — см. раздел 5, пункт 4 (ритм чередования).
- Видео — **новые**, сгенерированные отдельно от старых (`public/video/fish-*-swim.mp4`, «плывущая» рыба, не то же самое, что исходные `fish-*.mp4`, из которых просто вырезали стоп-кадр). Есть повторяющаяся путаница между «видео для стоп-кадра» и «видео для проигрывания» — если понадобится ещё раз, `fish-*-swim.mp4` это те, что реально показываются.
- **Пробовали и отклонили: тонировать видео в болотный цвет** (`mix-blend-mode:color` поверх, чтобы вода была ближе к фирменной палитре) — Босс попросил вернуть как было сразу же, естественный цвет воды. Не предлагать снова без явного запроса.
- **Пробовали и отклонили: горизонтальная карусель с 3×2 карточками-билетами и стрелками влево/вправо** (аналог стрелок в галерее hero) — сделано и работало технически, но Босс передумал на середине объяснения и попросил вернуть 3 карточки в ряд компактнее. Затем эта версия тоже была заменена на список+сцену. Если предложите карусель для рыб снова — уже пробовали, не понравилось не по техническим причинам, а по вкусу.

### CTA — сплошной блок с видео

Последняя секция главной, `STRUCTURE.md` §3.5. Тот же приём, что «О базе»: видео на всю ширину (`cta-trout.mp4`), тёмный градиент, контент поверх. Отличие — контент по центру в одну колонку (`max-width:var(--head-max)`), без второй колонки с карточками, потому что по структуре это просто «H2 + текст + кнопка», не более. Заголовок — дословно из структуры, **одноцветный** `--cream` (не двухцветный) — по той же причине, что и в «О базе»: на видео двухцветность не читается.

### Футер

**Обновлено 14.09.2026:** на фоне подвала едва заметный камуфляж — те же параметры, что у карточек «Быстрых фактов» (`camo.jpg`, 420px, ~4%, `saturate(.4)`, `screen`). Одинаково во всех `design/*.html`.

Тёмный (`--bog`), три колонки строго по `STRUCTURE.md` §2 (Контакты / Навигация — 4 пункта / О компании), низ — копирайт + дисклеймер. Текст колонки «О компании» — дословная цитата из структуры. Телефон — тот же плейсхолдер `+7 914 000-00-00`, что и everywhere on сайте (см. открытые вопросы к заказчику).

### Закреплённый хедер

`.site-header`, `position:fixed`, скрыт наверху (`transform:translateY(-100%);opacity:0`), появляется при скролле — JS-порог `scrollY > innerHeight * 0.6` (примерно после того, как hero прокручен). Фон — **болотный `--bog`, не камуфляж**: камуфляж на большую сплошную плашку уже пробовали и отклонили на этапе hero (раздел 2, раздел 6) — то же самое правило применили здесь не спрашивая, вместо камуфляжа предложили сразу болотный цвет, и это подошло. Компактнее, чем секция-хедер в hero (меньше паддинги, меньше кнопка) — по прямой правке Босса «хедер огромный, кнопку тоже уменьши».

### Анимация появления при скролле

**Усилено 16.09.2026 (Босс: «прикольную анимацию появления, счётчики, прелоадер»)** — общий блок CSS+JS во всех `design/*.html`:
- `.reveal` — подъём + лёгкое размытие (`filter:blur(6px)→0`).
- Заголовки `.h2`, `.page-head h1`, `.hero__brand` — скрипт оборачивает слова в `.w>span`, слова выезжают из-под линии по очереди (`--w`). Слова делятся только по обычным пробелам — неразрывные держат «с нами» одним словом.
- `.reveal-clip` — сцены с фото/видео (`.stay-stage`, `.scene`, `.contacts__scene`, `.day-panel__photo`, `.package-card__photo`, `.infra-card>img`, `.fish-stage`, `.shot-card__img`) раскрываются снизу вверх, кадр при этом наезжает 1.08→1. Класс вешает скрипт.
- Счётчики `[data-count]` (+`data-from`, `data-dur`): «Быстрые факты» 4 / 6 / 200 / 4 и «2005» в пропусках набирают число при появлении карточки.
- **Прелоадер** `.preloader`: болотный экран с камуфляжем, «КАВИНКА» антиквой собирается по буквам, под ней пунктирная леска с бегущей икринкой, подпись «Рыболовная база». Держится минимум 1.5 с, максимум 3.5 с, показывается **один раз за сессию** (`sessionStorage kv-pl`), при `prefers-reduced-motion` не показывается.


Общий класс `.reveal` (см. `:root` и правило `.reveal`/`.reveal.is-visible` в начале `<style>`) + один `IntersectionObserver` в конце `page.html`, применён почти ко всем текстовым/медиа-блокам на странице. Стагтер внутри группы (карточки фактов, пропуска, строки списка рыб) — через инлайновую CSS-переменную `style="--i:N"`, которая двигает `transition-delay`. Уважает `prefers-reduced-motion`.

**Техническая ловушка, если будешь трогать `.fish-list__item`:** у него `all:unset` (нужно было сбросить стили `<button>`) — это стирает `opacity`/`transform`, которые ставит `.reveal`, если `.reveal` определён раньше в каскаде (а он определён в начале файла, до всех секционных стилей). Решение — отдельное правило с более высокой специфичностью **после** `all:unset`: `.fish-list__item.reveal{...}`. Если где-то ещё понадобится `.reveal` на элементе с `all:unset` или другим сбросом стилей — тот же приём, не полагаться на порядок в файле.

### Единая сетка и шкала шрифтов — переменные в `:root`

Отдельный проход по всей главной, чтобы секции не отличались друг от друга «на глаз» (было: где-то шире контейнер, где-то у́же; где-то заголовок карточки 23px, где-то 31px, где-то 38px — без системы). Введены переменные:

```css
--grid-max: 1440px;   /* ширина контентной сетки */
--head-max: 620px;    /* ширина заголовочного блока (эйбрау+H2+лид) */
--size-h3: 24px;      /* заголовки крупных карточек, панелей и строк списков: рыбы, туры, дни программы (w600; в списках w500) */
--size-h3-sm: 20px;   /* заголовки узких карточек (4–5 в ряд) и тёмных полос: факты, пропуска, инфраструктура, «важная информация», трансфер */
--size-copy: 14.5px;  /* второстепенный текст внутри карточек, пункты программы, футер */
--size-small: 12.5px; /* третичные подписи: сезон, номер, «в сутки», бирки, подписи под фото */
--section-y: 120px;   /* верх/низ каждой секции — одинаково на всех страницах */
--head-gap: 64px;     /* заголовочный блок → контент */
--title-gap: 24px;    /* H2 → лид */
/* зазоры карточек (16.09): между карточками в сетке 22 на десктопе / 16 на телефоне;
   внутри карточки иконка → h3 30, h3 → текст 8; эйбрау → H2 всегда 18.
   Исключение — «пропуска» в «О базе»: 8 между ними, это перфорированная стопка. */
```

**Аудит 15.09.2026 (Босс: «привести иерархию текста в одну систему»):** до него шкала жила только на главной, а на внутренних страницах цифры разъезжались (секции 110–150 px, лиды 16–26 px, заголовки карточек 14.5 капсом / 16 / 19 / 26). Теперь на всех пяти `design/*.html` одна и та же лестница: **H1 60 · H2 58 (антиква капсом) · заголовок карточки 24 или 20 · лид 18 · текст 14.5 · подписи 12.5**, всё Manrope кроме H1/H2. Попутно сняли антикву там, где она стояла не по правилу §4: подписи под фото-карточками («О туре»), цитата «Дорога сюда — уже приключение», подпись в лайтбоксе — всё Manrope. Мелкие капс-подписи (сезон рыбы, «5 дней») — без разрядки, как эйбрау. На «Выбрать тур» у страницы не было H2 (стоял один текст 26 px) — добавлен H2 «Туры сезона», текст выше — лид 18.

Заводя новую секцию (в том числе на второй странице) — брать размеры и ширины отсюда, не подбирать новые числа на глаз. Если нужен принципиально другой масштаб — обсудить с Боссом и завести новую именованную переменную, а не одноразовое число внутри правила.

**Техническая ловушка при выравнивании полноэкранных видео-секций (`.about`, `.cta-section`):** если внутренний контент — flex-контейнер с `justify-content:space-between` (как `.about__content`, текст + карточки по разные стороны), и нужно ограничить его ширину `max-width` + `margin:auto` для центрирования на широких экранах — учитывай, что контент обычно живёт внутри уже «выбеленного» (bleed) стейджа шириной `calc(100% + 2×паддинг)`. Чтобы после центрирования плюс внутренний паддинг общий отступ от края совпадал с остальными секциями (46px + автоцентрирование = те же ~80px на 1600px экране), `max-width` должен быть `var(--grid-max) + величина bleed`, а не просто `var(--grid-max)` — иначе получится двойной отступ. Разобрались на практике на «О базе», см. историю в `DECISIONS.md` №17.

---

## Шапка внутренних страниц `.page-head` (14.09.2026)

Одна на все внутренние страницы, живёт в `design/about.html`, `pricing.html`, `accommodation.html` — CSS одинаковый, копировать дословно.

- **Компактная, ~320 px.** Большие пустые болотные плашки на пол-экрана Босс отклонил («огромные плашки, выглядит стрёмно»). Не возвращать.
- **Меню** — как на главной: лого слева, пункты по центру строчными, кнопка `.btn--sm` справа (бургер — только на узких экранах, см. «Единая шапка сайта»). Текущий раздел — оранжевая икринка 7 px под пунктом, не подчёркивание и не жирность.
- **Тело:** хлебные крошки «Главная • Раздел» (Manrope 13.5, без капса и разрядки) → H1 Cormorant 38–60 px капсом → подзаголовок Manrope 16.5.
- **Артефакт справа** — резьба с базы режимом `mix-blend-mode:screen` (фон у них чёрный, уходит сам). По странице: «О туре» — орёл, «Выбрать тур» — нерка, «Размещение» — медведь. Для следующих страниц брать другой из `public/photo/artifacts/`, не повторять.

**Единый формат шапки внутренних страниц — модификатор `.page-head--inner` (14.09.2026, по образцу «О туре»):** одинаково на `about.html`, `accommodation.html`, `pricing.html` и на будущих «Программа тура» и «Контакты». Крошки и H1 выровнены по левому краю ровно по логотипу «КАВИНКА» (`.page-head__body` без `max-width`, отступ 46/22 px), **подзаголовка нет** (`.page-head__sub` скрыт). Шапка ~270 px на ПК, камуфляж тот же. Резной артефакт справа, крупный, заезжает вверх под меню, подрезан снизу, `opacity:.6`; меню поднято над ним `z-index:2` на `.site-nav`. Размеры подогнаны под форму артефакта, чтобы визуально они были одного веса: орёл `width:min(640px,46vw)` · медведь (почти квадрат) `min(370px,27vw)`, сдвинут на 250 px от правого края, чтобы голова не уходила под кнопку «Оставить заявку» · нерка (вытянутая) `min(640px,46vw)`, опущена меньше. Для новых страниц брать другой артефакт из `public/photo/artifacts/`, не повторять. H1 в одну строку (`max-width:none`) — иначе длинные «Программа тура» переносились и шапка росла. Пробовали и отклонили: заголовок по центру с орлом-эмблемой за ним; шапка выше (~345 px, «громоздкая»).

**«О туре» → «Расположение базы» (14.09.2026):** текст вверху по центру (заголовок + оба абзаца, верхний отступ 80 px), ниже **плоский веер из трёх одинаковых карточек-поляроидов** (`.location__stack`, до 370 px): средняя стоит ровно и **лежит снизу**, боковые повёрнуты на ±8° вокруг своего центра, чуть ниже и **поверх средней — закрывают её углы** на ~16 px (просьба Босса); тень тонкая и плоская. Глубокий нахлёст за среднюю + дальняя точка поворота + тяжёлые тени давали «3D-эффект» — Босс отклонил. Карточка: матовый `--bog`, кремовая окантовка фото, подпись антиквой `--sage`. **Скотч:** по верхним углам, **плотный `--sage`** с тонкой кромкой `--olive`, без стекла и полосок; скотч только на боковых карточках (оба угла); у средней скрыт — её углы под боковыми, скотч торчал обрывками (полупрозрачный стеклянный вариант «не видно вообще», яркий оливковый с полосками — «инородно»). Нижний отступ секции 110 px. **Отклонено в этом блоке:** яркий оливковый скотч с полосками («инородное тело»); светлая полоска скотча по центру («потерялась»); метки на полях — штамп «Заповедник», «р. Тауй», «200 км от Магадана», компас «Север» — и круглая печать с координатами («мусор, лишние»). Декоративные метки сюда не возвращать. Кадры: нерпа · беседка «Кавинская долина» · знак заповедника. На ≤560 px карточки 2+1. **Отклонено:** белые поляроиды; брезент; дерево; полка; двухколоночный вариант; полукруг за карточками; **любые карты и схемы** — по центру, на всю секцию, диагональный маршрут с рельефом, прямая линия маршрута («бред, выглядит не очень»). Карту в этом блоке больше не предлагать.

**«О туре» → «Что включено в тур» (14.09.2026):** фон — песочный `#E5E3D1` + камуфляж 8.5% (как «Расположение базы»); шесть карточек `.inc-card` в три колонки **точь-в-точь в стиле «Быстрых фактов» с главной**: тёмный `--bog`, камуфляж ~4%, иконка-нашивка (кружок 66 px, кольцо + пунктир, значок `#D8CDA2`), заголовок Manrope 600, описание; эффекта наведения нет. Иконки: внедорожник · водомётная лодка с кильватером · сруб · гид в шляпе с удочкой · баня с паром и веником · беседка с мангалом. Над заголовком эйбрау с поплавком «В стоимости тура» (Босс попросил плашку «как везде»; прежний текст «Всё включено» убран на созвоне — не возвращать), лида нет. **Отклонено:** болотно-зелёный фон с кремовыми билетами-корешками; простые line-иконки.

**«Программа тура» — `design/program.html` (15.09.2026):** собрана целиком на компонентах «О туре», новых приёмов минимум. Состав: шапка `.page-head--inner` с артефактом **лось** → бегущая строка → **фото-сцена «Как устроен тур»** (компонент «Как добраться»: кадр `autumn-larches-wide.jpg`, текст слева-сверху, справа-снизу стеклянная лента «Дни 1–5 · база и рыбалка → День 6 · Магадан → Вылет · на следующий день») → **«Программа по дням»** (песочный + камуфляж): шесть строк `.day` — слева «День N», название Manrope и фото в тёмной рамке `.shot-card` (липнет при прокрутке), справа таймлайн `.steps` — пунктирная ось, точки-кружки, первая точка дня — оранжевая икринка, пометка «Есть возможность выбрать локальную экскурсию» — тёмная пилюля с меткой; фото по дням: аэропорт · рассвет у пирса · хариус · река Тауй · рыбак с уловом · паром → **«Важная информация»** — пять карточек `.inc-card` с иконками-нашивками (календарь, гид с группой, отель и самолёт, карта с меткой, посылка и удочка), сетка 3+2 → CTA → подвал. Во всех страницах ссылки «Программа тура» ведут на `program.html`. Текст — `STRUCTURE.md` §8.

**«О туре» → единый вертикальный ритм (14.09.2026):** все отступы страницы берутся из переменных в `:root`, отдельных «на глаз» в блоках нет: `--section-y:120px` (верх и низ каждой секции), `--head-gap:64px` (заголовочный блок → контент), `--title-gap:24px` (H2 → лид), `--lead-gap:14px` (лид → лид); эйбрау → H2 — 18 px из `.eyebrow`. На ≤1000 px: 88 / 44 / 20. Правила заданы один раз на `.section-head`; «Как добраться» и CTA используют те же токены. У ленты галереи паддинг под наклон/тень карточек гасится отрицательным полем, чтобы ритм совпадал с остальными блоками. CTA больше не держит фиксированную высоту `56svh`: слой видео `.cta-section__stage` стал `position:relative`, высоту задаёт контент с `--section-y` сверху и снизу (раньше низ обрезался до ~37 px). В «Расположении» ряд карточек выровнен по верху, чтобы средняя стояла ровно на `--head-gap`. **Соседние блоки на одном фоне:** секции с песочным фоном помечены классом `.sand`; правило `.sand+.sand{padding-top:0}` — иначе нижний и верхний `--section-y` складывались в пустоту 240 px («Что включено» → «Галерея», Босс: «огромный отступ, не подчиняется правилам»). Между любыми блоками — ровно один `--section-y`. Босс: «одинаковые отступы, чтобы не казалось, что блоки разные». Новые секции собирать только на этих токенах.

**«О туре» → «Галерея» (14.09.2026):** фон песочный + камуфляж 8.5% (как «Расположение» и «Что включено»); эйбрау «В дороге и на месте» + H2. Фото — **бесконечная лента** (`.gallery__marquee` / `.gallery__track`, CSS-анимация 200 с, пауза на наведении, мягкое затухание по краям маской); карточки — те же `.shot-card` (тёмная рамка, кремовая окантовка, подпись антиквой `--sage`), `clamp(300px,26vw,400px)` — Босс: «маленькая галерея», наклон ±1.5° и лёгкий сдвиг по высоте через одну. Вторая половина ленты — копия с `data-clone aria-hidden`, сдвиг `calc(-50% - 17px)` (половина зазора 34 px) делает стык невидимым. ~26 кадров, **строго через один: рыбалка/люди с уловом → вид базы или природы → рыбалка → вид…** (осенняя тайга, база с воды, рассвет у пирса, закат, купель у реки, вечерний пирс, вид из окна, орлан), транспорт — один кадр. Босс: «люди едут рыбачить и смотреть красоты, а не на вертолёт», «чередуй, чтобы не одна рыбалка». Анимация 200 с. При `prefers-reduced-motion` лента стоит и листается вручную. Отклонено: статичный ряд из пяти белых поляроидов на `--cream-warm`.

**Лайтбокс — общий на весь сайт (14.09.2026):** любое фото в карточке/рамке открывается на весь экран: курсор-лупа, затемнённый фон `rgba(14,18,10,.93)`, подпись антиквой `--sage` (из `figcaption`, иначе `alt`), счётчик «3 / 17», стрелки ← → и кнопки-кружки в стиле сайта, Esc и клик по фону закрывают. CSS и скрипт **одинаковые** во всех `design/*.html` (блок «ЛАЙТБОКС»). Декоративные картинки исключены селектором `EXCLUDE` в скрипте (`.page-head__art`, `.route__bg`, `.extras__art`, `.quote__art`, `.hero img`, `[data-nozoom]`) — новую декоративную картинку помечать `data-nozoom`. Копии в бегущей ленте открывают оригинал, в пролистывании не дублируются.

**«О туре» → «Как добраться» (14.09.2026):** полноэкранная сцена как hero/CTA поверх кадра `river-boat-hills.jpg`; элементы разведены по углам, чтобы не закрывать лодку в центре кадра: текст — левый верх (`.route__text`), лента маршрута и кнопка — правый низ (`.route__aside`); затемнение только в этих двух углах (центрированный вариант Босс отклонил — «картинка перекрывалась») (`.route__bg` — заказчик подставит `<video>` тем же классом). Состав: эйбрау → H2 → цитата «Дорога сюда — уже приключение» крупной прямой антиквой (курсив Босс убрал) (исключение из §4, как вывеска) → лид → **стеклянная лента маршрута** «Магадан · аэропорт → 200 км → Кавинка · река Тауй» (пилюли в языке `.coords-pill`, между ними пунктир с оранжевой икринкой) → `.glass-btn`. Список шагов «Автомобиль / Паром / Вертолёт» с миниатюрами Босс убрал — транспорт остаётся только в лиде.

**Две ловушки:**

1. У текста и `.page-head__body` **нельзя ставить `z-index`**. Иначе создаётся свой слой, `screen` смешивается с прозрачностью, и вместо резьбы виден чёрный прямоугольник.
2. **Маска на края — только у орла** (`--eagle`), у него в исходнике виден шов подсветки. У нерки и медведя маска съедает голову и хвост, им она не нужна.

---

## Единая шапка сайта `.site-nav` + `.site-header` (14.09.2026)

По прямой просьбе Босса: «единый хедер для всего сайта», «всё меню к одному виду». Разметка и CSS **совпадают дословно** в `design/page.html`, `about.html`, `accommodation.html`, `pricing.html` — при правке менять во всех четырёх, либо сразу выносить в общий компонент при переносе в React.

**Пункты меню — пять, в этом порядке** (итоговое меню, утверждено Боссом 14.09.2026; то же в `lib/site.ts`):
О туре → `about.html` · Размещение → `accommodation.html` · Выбрать тур → `pricing.html` · Программа тура → `#` · Контакты → `#` (последних двух страниц пока нет).
«Расписание» из меню убрано, вкладки «Тур» и «Стоимость» больше не существуют — не возвращать.
Тот же список в трёх местах: меню первого экрана, закреплённая шапка, футер.

- **`.site-nav`** — меню первого экрана: сетка `1fr auto 1fr`, логотип слева, пункты по центру строчными (Manrope 14.5), справа `.btn--sm` (+ бургер только ≤1120 px). Текущий раздел — оранжевая икринка под пунктом (`a.is-current`). На главной поверх видео — модификатор `.site-nav--overlay`, текущего пункта нет.
- **`.site-header`** — та же `.site-nav` внутри фиксированной болотной полосы, компактнее по отступам. Выезжает, когда первый экран прокручен: порог `firstScreen.offsetHeight - 60`, где `firstScreen` — `.hero` на главной или `.page-head` на внутренних. Раньше порог на каждой странице был свой.
- **Бегущая строка** — сразу под первым экраном на всех страницах, тексты одни и те же.
- **Бургер — только на узких экранах.** На ПК (>1120 px) его нет: `.site-nav__burger{display:none}`, показывается в `@media(max-width:1120px)` вместе с тем, как прячутся пункты. Правило Босса 14.09.2026 — «на ПК он не нужен».
- До 1120 px пункты прячутся в бургер, логотип и кнопка остаются: пять пунктов рядом с логотипом и кнопкой на ~1024 px не помещаются, кнопка переносилась на две строки. Отступы по краям сужаются только с 980 px. Надпись кнопки в шапке не переносится (`white-space:nowrap`).

**Первый экран:** на внутренних страницах — `.page-head` (см. раздел выше). Главная сохраняет полноэкранное видео — это утверждённый макет заказчика, под общую шапку переведено только меню.
